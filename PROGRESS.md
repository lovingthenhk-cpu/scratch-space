# PROGRESS

## 現在のテーマ

コラッツ予想 (Collatz conjecture, 3x+1問題) と、その一般化 ($qn+1$ 写像)
— 数学の未解決問題への挑戦トラック。

## これまでの進捗 (最終更新: 2026-07-26)

### 2026-07-24 (初回)

1. **Part A: 全数検証** — $n \in [1, 10^9)$ の全整数についてコラッツ予想が
   成立することを検証。反例なし。記録保持者: $n=670617279$, 986ステップ。
   平均ステップ数 203.23。
2. **Part B: 奇数ステップ比率の実証** — 標準ヒューリスティック
   ($o/(o+e) \to 1/(1+\log_2 3) \approx 0.386853$) への収束を、約76,000+
   長軌道1042サンプルで確認(900〜1100ステップ帯で理論値との差0.023まで
   縮小)。

詳細は `2026-07-24/REPORT.md` 参照。

### 2026-07-25 (前日の続き)

1. **Part A拡張**: 全数検証範囲を $10^9 \to 7\times10^9$ に拡張。反例なし。
   新記録: $n=4890328815$, **1131ステップ**。$[1,7\times10^9)$ 全体平均
   ステップ数 223.53。(並列シャーディング時の記録受け渡しの注意点あり。
   詳細は `2026-07-25/REPORT.md` のPart A'節参照。)
2. **Part D: 記録列の回帰分析**: steps ≈ 53.72·ln(n) − 175.33 (R²=0.959,
   n=71件)。
3. **Part C: 一般化 $qn+1$ 写像の実験**: $q=3,5,7,9,11,13$ でランダム
   サンプリング → $q\geq5$ でほぼ全ての軌道が発散することを確認。
   $q=5$ について odd $n\leq3\times10^6$ を網羅的にスイープし、
   **2つの異なる非自明10-サイクル**を発見:
   $\{13,66,33,166,83,416,208,104,52,26\}$ と
   $\{17,86,43,216,108,54,27,136,68,34\}$。$q=7,9,11,13$ では同範囲に
   非自明サイクルは見つからなかった。

詳細は `2026-07-25/REPORT.md` を参照。

### 2026-07-26 (前日Part Cの続き + Part Dの追加分析)

前日の宿題のうち優先度1・2に着手(優先度3の全数検証拡張、優先度4の
長軌道ハント改善は未着手)。

1. **`sweep_cycles.c` の高速化**: 前日のサイクル探索コードは毎回
   ハッシュテーブルを `memset` で全クリアしており、$3\times10^6$ 超の
   スイープが非現実的だった。「epoch(世代)タグ付き」ハッシュテーブル
   (`2026-07-26/sweep_cycles_v2.c`)に書き換え、テーブルクリアを
   $O(1)$ に。実測で100倍以上高速化(約100万件/秒/コア、$q=5$)。
   旧実装との結果一致を確認済み。
2. **バグ発見・修正**: 高速化版で再検証中、$q=13$ で32個の
   "inconclusive"(旧実装・新実装で完全一致)を発見。デバッグの結果、
   「オーバーフロー防止ガード」と「通常の発散閾値判定」という2つの
   発散判定の間で `step > DIVERGE_MIN_STEPS`(=30)という境界条件が
   食い違い、明らかに発散しているトラジェクトリ(乗算を経て
   $10^{17}$ の45倍以上に達している)を誤って「結論不能」に分類して
   いたoff-by-oneバグだと判明。オーバーフローガード分岐を無条件で
   `DIVERGED` を返すように修正し、修正前後で `diverged` がちょうど
   32件増加・`inconclusive` が0になることを確認。詳細は
   `2026-07-26/REPORT.md` のPart A'節参照。
3. **Part B拡張**: 修正済み `sweep_cycles_v2` で $q=5,7,9,11,13$ の
   非自明サイクル探索範囲を $3\times10^6 \to 5\times10^8$(約167倍)に
   拡張。$q=5$ で見つかったのは前日と**同じ2つの**10-サイクルのみ
   (新規なし)。$q=7,9,11,13$ では非自明サイクルは1つも見つからず。
4. **Part D追加: 記録列の極値統計分析**(新規、軽量、既存データのみ
   使用): 71件のglide record($n$ vs 記録steps)について、
   (a) 記録個数 $K$ と $\ln N$ の比 $K/\ln N \approx 3.13$ ——
   「i.i.d.列の記録個数は $\sim\ln N$」という普遍法則の予測(比=1)の
   約3.1倍。近傍$n$同士のトラジェクトリ相関が交換可能性を壊している
   可能性を考察。
   (b) 記録間の対数間隔 $\ln(n_{k+1}/n_k)$ の標準偏差/平均 $=0.623$
   (指数分布なら理論上1)。KS検定で「指数分布」を5%水準で棄却
   —— 記録はポアソン過程が予測するよりも規則的に並んでいる。
   詳細・図は `2026-07-26/REPORT.md` および
   `2026-07-26/extreme_value_analysis.png` 参照。

詳細は `2026-07-26/REPORT.md` を参照。

## 次回やろうとしていたこと

優先度順:

1. **一般化写像のサイクル探索をさらに拡張**(2026-07-26 Part B の
   直接の続き)。修正済み `sweep_cycles_v2.c` は高速なので、特に
   $q=5$($5\times10^8$までは既知の2サイクルのみ)を $10^{10}$ 程度
   まで伸ばすのは数十分程度で現実的。$q=7,9,11,13$ は既に
   $5\times10^8$までゼロ件なので優先度はやや下げてよいが、同時に流す
   分にはコストはほぼゼロ。

2. **記録の対数間隔が「指数分布より規則的」という発見の深掘り**
   (2026-07-26 Part D の直接の続き)。$n=70$ サンプルでの結果なので、
   記録数が増えれば(優先度3参照)この統計量の収束先を追える。
   Weibull・Gamma(shape>1) など他の分布族でのフィット比較も追加分析
   として軽量にできる。

3. **Part Aの全数検証範囲をさらに伸ばす**。$7\times10^9$ から
   $10^{10}$〜$2\times10^{10}$ 程度へ。並列シャーディングを使う場合は、
   2026-07-25 で気づいた「並列シャード間の記録閾値受け渡し」の問題を
   最初から正しく設計すること(逐次実行にするか、後処理で必ず補正する
   スクリプトを用意する)。新しい記録が見つかれば優先度2の分析データも
   自然に増える。

4. **長軌道ハントの改善**(2026-07-24 から3回持ち越し、まだ未着手)。
   今は完全ランダム探索でステップ数700以上をフィルタしているだけ。
   記録保持者 (glide records) の近傍や peak/n比が大きい数の近傍を
   集中的に探索するヒューリスティック探索に変えると、2000ステップ超の
   軌道サンプルをもっと効率的に集められる可能性がある。

どれから手をつけてもよい。1は今回の続きで低コスト、2は軽量な追加分析、
3・4は持ち越し。

## 環境メモ

- サンドボックスは毎回まっさら。C コンパイラ(gcc 13.3, `-O3 -march=native`
  で問題なく動く)、Python3.11 + pandas/numpy/matplotlib が使える
  (`pip install --break-system-packages` が必要な場合あり)。
- 2コア, 8GB RAM 程度のマシンイメージ。$10^9$ 規模の全数検証はシングル
  スレッドで数分で終わる。実測スループット: 約6.5×10^6 個/秒/コア
  (単純な全数検証)、約1×10^6 件/秒/コア(サイクル探索、epoch方式
  ハッシュテーブル使用時、$q=5$)。
- **バックグラウンド実行のコツ**: 長時間かかる計算(数分〜十数分)は
  `nohup ./prog args > log 2>&1 &` でバックグラウンドに投げ、`ps` で
  ポーリングしながら他の作業(別の実験、分析スクリプト作成など)を
  並行して進めると時間を無駄にしない。複数プロセスを並列実行して
  「記録」のような累積的な状態を追跡する場合は、「並列シャード間の
  閾値受け渡し」問題に注意すること。
- **`Bash`ツールのタイムアウト**: 明示的に`timeout`パラメータを渡さない
  限りデフォルトは2分。長めに`sleep`したい場合は`timeout`パラメータを
  必ず明示的に指定すること(指定し忘れると2分でコマンドごと打ち切られる、
  バックグラウンドプロセス自体は生き続けるので実害はないが無駄な
  やり取りが増える)。
- **「毎回全クリアする一時テーブル」パターンを見たら epoch/世代タグ付け
  を疑う**: 2026-07-26 で `sweep_cycles.c` → `sweep_cycles_v2.c` の
  高速化(100倍以上)に使ったテクニック。ループごとに大きな配列を
  `memset` しているコードを見つけたら、まずこれを疑うと良い。
