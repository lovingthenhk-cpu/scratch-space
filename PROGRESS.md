# PROGRESS

## 現在のテーマ

コラッツ予想 (Collatz conjecture, 3x+1問題) — 数学の未解決問題への挑戦トラック。

## これまでの進捗 (最終更新: 2026-07-24, 初回)

`2026-07-24/` に以下を実施:

1. **Part A: 全数検証** — `verify.c` (メモ化つきC実装) で $n \in [1, 10^9)$
   の全整数についてコラッツ予想が成立する(1に到達する)ことを検証。反例なし。
   - 記録保持者(ステップ数最大): $n=670617279$, 986ステップ。
   - 平均ステップ数: 203.23。
   - `glide_records.csv`, `peak_records.csv` に記録更新の履歴あり。
   - 実行時間 約2分、メモ化テーブルサイズ $2\times10^7$。

2. **Part B: 奇数ステップ比率の実証** — 標準的なヒューリスティック
   ($o/(o+e) \to 1/(1+\log_2 3) \approx 0.386853$、$o$=奇数ステップ数,
   $e$=偶数ステップ数)を、ランダムサンプリング(約76,000サンプル、
   $10^3$〜$10^{18}$)+長軌道ハント(1042サンプル、ステップ数700以上のみ採用)
   で検証。ステップ数が増えるにつれ理論値へ単調に収束する傾向を確認
   (900〜1100ステップ帯で理論値との差0.023まで縮小)。
   - `ratio_sample.c` でサンプリング、`analyze.py` で集計、`plot.py` で
     `odd_fraction_convergence.png` を生成。

詳細は `2026-07-24/REPORT.md` を参照。

## 次回やろうとしていたこと

優先度順:

1. **Part Aの検証範囲を伸ばす**。現状 $10^9$ まで。メモ化サイズと実行時間の
   バランスを見つつ $10^{10}$〜$10^{11}$ 程度まで伸ばせないか試す
   (2コア環境、シングルスレッドで $10^9$ が約2分だったので、素朴に
   スケールすると $10^{10}$ で約20分、$10^{11}$ で約3〜4時間程度になる
   見込み。並列化 (2コアでのマルチスレッド化 or 範囲分割) を検討する価値あり)。

2. **長軌道ハントの改善**。今は完全ランダム探索でステップ数700以上を
   フィルタしているだけ。記録保持者 (glide records) の近傍や、
   peak/n比が大きい数の近傍を集中的に探索するヒューリスティック探索に
   変えると、2000ステップ超の軌道サンプルをもっと効率的に集められる
   可能性がある。これができれば「奇数ステップ比率の収束」グラフの
   右端をもっと伸ばせる。

3. **一般化コラッツ写像への拡張**。$3n+1$ の代わりに $5n+1$, $7n+1$ など
   $qn+1$ ($q$: 奇数, $q\ge5$)を試す。理論的には $q\ge5$ では発散する
   軌道が存在しうることが知られている(sqrt(2)<... の条件、要再確認)ので、
   実際にランダムサーチで発散軌道(ピーク値が際限なく増え続けるもの)を
   探してみると面白い実験になりそう。既存の `verify.c` / `ratio_sample.c`
   の $T(n)$ 部分を一般化すれば流用できる。

4. **記録ホルダー列の回帰分析**。`glide_records.csv` の $n$ vs steps を
   $\log n$ に対して回帰し、平均的な増加率の傾き(既知の経験則
   ステップ数 $\approx c \cdot \ln n$ の $c$)を自分のデータから推定する。

上記のどれから手をつけてもよい。1と2は直接の続き、3は新しい発展方向、
4は既存データの追加分析で軽量に着手できる。

## 環境メモ

- サンドボックスは毎回まっさら。C コンパイラ(gcc 13.3, `-O3 -march=native`
  で問題なく動く)、Python3.11 + pandas/numpy/matplotlib が使える
  (`pip install --break-system-packages` が必要な場合あり)。
- 2コア, 8GB RAM 程度のマシンイメージ。$10^9$ 規模の全数検証は
  シングルスレッドで数分で終わる。
