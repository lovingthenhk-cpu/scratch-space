# PROGRESS

## 現在のテーマ

コラッツ予想 (Collatz conjecture, 3x+1問題) と、その一般化 ($qn+1$ 写像)
— 数学の未解決問題への挑戦トラック。

**2026-07-28時点の焦点**: Sultan (2020, arXiv:2005.00346) が明記した
「$q=5$ の $T_5$ 写像で $h=4$($n\equiv4\pmod5$)の合同類にサイクルが
存在するか」という具体的な未解決問題に対し、**2つの独立な手法**を並行
して伸ばしている: (1) トラジェクトリベースの値の総当たり(`sweep_cycles_v2.c`、
$n_0$ を伸ばして下界を更新)、(2) 2026-07-28に新規導入した**サイクル方程式
による形状ベースの完全網羅**(`cycle_equation.py`、奇数項の個数 $s$ と
総サイクル長 $A$ の範囲を固定して全形状を厳密に(浮動小数点なし)排除する、
存在しないことの"証明"に到達できる唯一の手法)。

## これまでの進捗 (最終更新: 2026-07-28)

### 2026-07-24 (初回)

1. **Part A: 全数検証** — $n \in [1, 10^9)$ の全整数についてコラッツ予想が
   成立することを検証。反例なし。記録保持者: $n=670617279$, 986ステップ。
   平均ステップ数 203.23。
2. **Part B: 奇数ステップ比率の実証** — 標準ヒューリスティック
   ($o/(o+e) \to 1/(1+\log_2 3) \approx 0.386853$) への収束を、約76,000+
   長軌道1042サンプルで確認(900〜1100ステップ帯で理論値との差0.023まで
   縮小)。

詳細は `2026-07-24/REPORT.md` 参照。

### 2026-07-25 (前日の続き)

1. **Part A拡張**: 全数検証範囲を $10^9 \to 7\times10^9$ に拡張。反例なし。
   新記録: $n=4890328815$, **1131ステップ**。$[1,7\times10^9)$ 全体平均
   ステップ数 223.53。
2. **Part D: 記録列の回帰分析**: steps ≈ 53.72·ln(n) − 175.33 (R²=0.959,
   n=71件)。
3. **Part C: 一般化 $qn+1$ 写像の実験**: $q=3,5,7,9,11,13$ でランダム
   サンプリング → $q\geq5$ でほぼ全ての軌道が発散することを確認。
   $q=5$ について odd $n\leq3\times10^6$ を網羅的にスイープし、2つの
   非自明10-サイクルを発見(**注: 2026-07-27に判明、これは実は既知だった
   —— 下記参照**)。$q=7,9,11,13$ では同範囲に非自明サイクルは見つからな
   かった。

詳細は `2026-07-25/REPORT.md` を参照。

### 2026-07-26 (前日Part Cの続き + Part Dの追加分析)

1. **`sweep_cycles.c` の高速化**: epoch(世代)タグ付きハッシュテーブル
   (`sweep_cycles_v2.c`)で100倍以上高速化(約100万件/秒/コア、$q=5$)。
2. **バグ発見・修正**: オーバーフロー防止ガードと発散判定の間の
   off-by-oneバグを修正(詳細は `2026-07-26/REPORT.md`)。
3. **Part B拡張**: $q=5,7,9,11,13$ の非自明サイクル探索範囲を
   $3\times10^6 \to 5\times10^8$ に拡張。$q=5$ で見つかったのは前日と
   同じ2つの10-サイクルのみ(新規なし)。$q=7,9,11,13$ では非自明サイクル
   は1つも見つからず。
4. **Part D追加: 記録列の極値統計分析**: 記録個数 $K$ と $\ln N$ の比
   $K/\ln N \approx 3.13$(普遍法則予測の約3.1倍)。記録間の対数間隔の
   標準偏差/平均 $=0.623$、KS検定で「指数分布」を5%水準で棄却
   —— 記録はポアソン過程が予測するよりも規則的に並んでいる。

詳細は `2026-07-26/REPORT.md` を参照。

### 2026-07-27 (★重要な軌道修正: 先行研究確認 → q=5サイクル探索の再定義)

今回から「お題を選ぶ前に必ず先行研究を確認する」運用ルールが導入されたため、
まず前日までの続きである「$q=5$ 非自明サイクル探索」自体を検証した。

1. **Part E(最重要・訂正)**: WebSearch/WebFetchで確認した結果、
   Sultan, A. **"On the Collatz general problem qn+1"** (arXiv:2005.00346,
   2020) の Table 1・Remark 3 に、私たちが2026-07-25に「発見」したと
   書いた2つの $q=5$ 非自明10-サイクル
   ($\{13,26,33,52,66,83,104,166,208,416\}$ と
   $\{17,27,34,43,54,68,86,108,136,216\}$)が、**同一のサイクルとして
   既に発表済み**であることが判明した(論文はショートカット表記で
   7要素として記載しているが、完全軌道を書き出すと要素の並び順まで
   完全一致する)。「新規発見」という過去の記述は不正確だったので
   ここで訂正する。詳細と検証根拠は `2026-07-27/REPORT.md` Part E 参照。
   **教訓として今後も参照すること**: これはまさに今回追加された運用ルール
   (先行研究確認の必須化)が防ごうとしていた失敗パターンの実例。
2. **Part F(方向転換)**: 幸い、Sultan (2020) の Remark 3 は
   「$q=5$ の $T_5$ 写像には、$n\equiv4\pmod5$($n\equiv9\pmod{10}$)の
   合同類に**存在するとすれば高々もう1つ**の非自明サイクルがありうるが、
   その存在・非存在は不明」と明記しており、これは正真正銘、具体的で狭い
   未解決問題である(論文にもfollow-up文献にも解決の記述なし、かつ
   探索済み範囲の記載も文献中に見当たらない)。この $n\equiv9\pmod{10}$
   条件は私たちの全数スイープ($q=5$の全ての奇数nを試す)に自動的に
   含まれているため、既存の `sweep_cycles_v2.c` をそのまま使い、
   今回は狙いを $q=5$ に絞って2コアを最大限投入した:
   - shard A: odd $n\in[5\times10^8+3, 3\times10^9]$
   - shard B: odd $n\in[3\times10^9+3, 5.5\times10^9]$

   両シャード完走(各約50-56分のCPU時間)、**新しいサイクルは1件も
   見つからなかった**(既知の2サイクル以外なし)。これにより $q=5$ の
   非自明サイクル探索範囲が $5\times10^8\to5.5\times10^9$(**11倍**)に
   拡張された。ただしこれは Sultan (2020) の未解決問題を「解決した」もの
   ではなく、「存在するとしても最小代表元は $5.5\times10^9$ より大きい」
   という具体的な下界を独自に得た、というのが正確な言い方(論文自体には
   探索範囲の記載がなかったため、この数値がどれだけ「新しい」情報かは
   不明)。詳細は `2026-07-27/REPORT.md` Part F 参照。
3. **Part G(優先度2の続き、軽量)**: 2026-07-26で見つけた「記録の対数
   間隔が指数分布より規則的」という観察を、scipyでGamma/Weibull/Lognormal
   フィット+KS検定+AIC比較で定量化。Weibull(shape≈1.66)とGamma
   (shape≈2.31)がExponentialより有意に良い当てはまり(AIC差+21前後)。
   Gammaのshapeパラメータのブートストラップ90%信頼区間は $[1.89,3.09]$
   (n=70の記録数ではまだ幅がある)。詳細は `2026-07-27/REPORT.md` Part G、
   図は `2026-07-27/loggap_distribution_fit.png` 参照。

詳細は `2026-07-27/REPORT.md` を参照。

### 2026-07-28 (前日Part Fの続き + Part H: サイクル方程式探索を新規導入)

1. **Part F続行**: `sweep_cycles_v2.c`(変更なし)で $q=5$ の全数
   スイープを2シャード(各2コア)で延長: $[5.5\times10^9, 8\times10^9]$
   と $[8\times10^9, 1.05\times10^{10}]$。両シャード完走、**新しい
   サイクルは1件も見つからず**(既知の2サイクルのみ)。トラジェクトリ
   ベースの探索範囲は $5.5\times10^9 \to 1.05\times10^{10}$(約1.9倍)
   に拡張された。
2. **Part H(新規手法)**: 「値の総当たり(トラジェクトリ探索)はどこまで
   行っても存在しないことの証明にならない」という2026-07-27時点の限界
   を踏まえ、**サイクル方程式による形状(shape)ベースの完全網羅探索**を
   新規実装(`cycle_equation.py`)。長さ $s$ のサイクルを奇数項の後の
   halving回数の組 $(a_1,\ldots,a_s)$ でパラメータ化し、サイクルが閉じる
   ための唯一の $n_1 = c_{s+1}/(2^A-q^s)$ を厳密な整数/有理数演算で解く
   (浮動小数点なし)。既知の3サイクル(自明サイクル $n_1=1$、非自明
   サイクル $n_1=17,13$)を正しく再現することでロジックを検証済み。
   $s=1$ から $s=9$ まで(各 $s$ につき $A$ を表 [`2026-07-28/REPORT.md`
   Part H参照] の範囲で)**全形状を完全に網羅**し、合計
   **21.98億通り**の形状を検証、$n_1\equiv4\pmod5$($h=4$)の整数解は
   **1件も存在しない**ことを確認(候補が出れば独立な順方向シミュレーション
   で二重検証する仕組みも実装済み、今回は候補ゼロだったため出番なし)。
   これはトラジェクトリ探索とは独立な軸(「サイクルの構造」)での網羅性で
   あり、例えば「奇数項2個の $h=4$ サイクルは総長10002以下には値によらず
   存在しない」という、値ベース探索では出せない結論を厳密に示せている。
   ただし $s\geq10$ の領域はほぼ手付かずで、組合せ爆発($\binom{A-1}{s-1}$)
   のため単純な全探索の限界にすでに達している。詳細と限界の正確な記述は
   `2026-07-28/REPORT.md` Part H を参照。

詳細は `2026-07-28/REPORT.md` を参照。

## 次回やろうとしていたこと

優先度順:

1. **サイクル方程式探索(Part H)の高度化**(2026-07-28 新規導入の直接
   続き、優先度を最も高くする——単純な形状の全探索は $s\geq10$ で
   組合せ爆発により手詰まりになったため、次の一手が必要): 候補は
   (a) meet-in-the-middle(形状を前半・後半に分割し $(c,b)$ 対を
   ハッシュテーブルで突き合わせる、計算量を $\binom{A-1}{s-1}$ から
   その平方根程度まで落とせる可能性)、(b) 連分数・Diophantine近似
   ($\log_2 5$ の無理数度)を使って「$n_1$ が有界であるためにはどの
   $(s,A)$ の組しか有効になり得ないか」を解析的に絞り込む手法(3x+1問題
   の非自明サイクル非存在証明で実際に使われている考え方のはず、要文献
   確認——検索してから実装すること)。どちらも今回の担当分より数論的に
   踏み込む必要があるので、時間があればまず関連文献をあたってから着手
   するとよい。
2. **$q=5$ の $h=4$ トラジェクトリ探索をさらに延長**(2026-07-28 Part F
   の直接続き)。$1.05\times10^{10}$ から先へ。同じ `sweep_cycles_v2.c`
   を使う。ただし今回気づいた通り、ホスト側のCPUクォータが変動すること
   があるので(2026-07-28環境メモ参照)、所要時間の見積もりには余裕を
   持たせること。優先度は1より低い——値ベースの下界を伸ばすだけでは
   Part Hのような「証明」には到達できないため、Part Hの深掘りを優先する
   方針にした。
3. **記録の対数間隔の分布フィットの深掘り**(2026-07-27 Part G の続き)。
   $n=70$ サンプルでの結果なので、記録数が増えれば(優先度5参照)
   ブートストラップ信頼区間を狭められる。
4. **$q=7,9,11,13$ のサイクル探索も同程度まで延長**(優先度は $q=5$ より
   低い —— 文献上どちらも「未解決」と名指しされてはいないため)。
5. **Part Aの全数検証範囲をさらに伸ばす**。$7\times10^9$ から
   $10^{10}$〜$2\times10^{10}$ 程度へ。並列シャーディングを使う場合は、
   2026-07-25 で気づいた「並列シャード間の記録閾値受け渡し」の問題に
   注意すること(逐次実行にするか、後処理で必ず補正するスクリプトを
   用意する)。新しい記録が見つかれば優先度3の分析データも自然に増える。
6. **長軌道ハントの改善**(2026-07-24 から5回持ち越し、まだ未着手)。
   今は完全ランダム探索でステップ数700以上をフィルタしているだけ。
   記録保持者 (glide records) の近傍や peak/n比が大きい数の近傍を
   集中的に探索するヒューリスティック探索に変えると、2000ステップ超の
   軌道サンプルをもっと効率的に集められる可能性がある。

どれから手をつけてもよい。1は今回新規導入した手法の最も自然な深掘りで、
かつ「証明」に近づける唯一の方向性なので優先度を最も高くした。2〜3は
軽量〜中量の持ち越し、4〜6はさらに持ち越し。

## 過去に検討して見送った候補(繰り返し検討しないこと)

- **「$q=5$ の非自明サイクルを新規に発見する」という枠組み自体**
  (2026-07-25〜26の元々の理解)は2026-07-27に見送り、
  「Sultan (2020) の未解決の $h=4$ 合同類サイクル探索を続行する」という
  より具体的で文献的裏付けのある枠組みに置き換えた。今後は必ず
  「$h=4$ / $n\equiv9\pmod{10}$ のサイクル探索」という文脈で語ること。
  「新規サイクル発見」という表現は使わないこと($h=2,3$ に対応する
  2つのサイクルは既知であるため)。

## 環境メモ

- サンドボックスは毎回まっさら。C コンパイラ(gcc 13.3, `-O3 -march=native`
  で問題なく動く)、Python3.11 + pandas/numpy/scipy/matplotlib が使える
  (`pip install --break-system-packages` が必要な場合あり)。
- 2コア, 8GB RAM 程度のマシンイメージ。$10^9$ 規模の全数検証はシングル
  スレッドで数分で終わる。実測スループット(2026-07-27時点、専有1コア):
  $q=5$ のサイクル探索で約 **4.1×10^5 件/秒/コア**(2026-07-26に書いた
  「約1×10^6件/秒/コア」は恐らくマシンイメージやコア占有状況の違いで
  今回は再現しなかった。**新しいセッションでは必ず小規模ベンチマークで
  実測すること**。目安: `time ./sweep_v2 5 20000000 1` で約24秒なら
  約4×10^5件/秒/コア)。
- **バックグラウンド実行のコツ**: 長時間かかる計算(数十分)は
  `nohup ./prog args > log 2>&1 &` でバックグラウンドに投げ、`ps` で
  ポーリングしながら他の作業(分析スクリプト作成、レポート執筆など)を
  並行して進めると時間を無駄にしない。**stdout はプログラム終了時に
  まとめて出力される(バッファリングされる)ので、ログファイルの中身は
  プロセスが完全に終わるまで空のまま** —— `ps`でCPU時間を見て進捗を
  推測すること。2プロセスを2コアに1つずつ割り当てる場合、`nohup` で
  同時に2つ立ち上げれば OS が自動的にほぼ均等に割り振る(今回は
  各98%CPU使用率で安定)。
- **`Bash`ツールのタイムアウト**: 明示的に`timeout`パラメータを渡さない
  限りデフォルトは2分、最大10分(600000ms)。50分規模の待機が必要な
  場合は `sleep 570`(9分30秒)程度を`timeout: 600000`と組み合わせて
  複数回に分けて呼ぶこと。
- **★2026-07-28で得た教訓: CPUクォータは変動する、複数の重い処理の同時
  実行は見積もりを大きく外しうる**: 今回、C言語の重いシャード2本(2コア
  想定)に加えてPython(サイクル方程式の全探索)も同時に走らせたところ、
  3プロセス合計のCPU使用率が理論上限200%ではなく117〜205%の間で変動した
  (`ps aux --sort=-%cpu` で確認)。`nproc`が2を返しても、共有クラウド
  基盤側のクォータは常に2コア分もらえるとは限らないらしい。結果、前日
  48〜56分だった同規模シャードが今回は62分・61分のCPU時間(壁時計では
  83分・101分)かかった。**教訓**: 複数の重い処理(C言語のバックグラウンド
  ジョブ + Pythonの重い計算など)を同時に走らせる計画を立てるときは、
  見積もり時間に余裕(1.5〜2倍程度)を持たせること。可能なら「重い処理は
  1つずつ、順番に」の方が予測可能。
- **「毎回全クリアする一時テーブル」パターンを見たら epoch/世代タグ付け
  を疑う**: 2026-07-26 で `sweep_cycles.c` → `sweep_cycles_v2.c` の
  高速化(100倍以上)に使ったテクニック。
- **★最重要(2026-07-27で得た教訓)**: 「非自明な発見をした」と書く前に、
  必ずWebSearch/WebFetchでその具体的な数値・対象(この場合は「サイクルの
  要素の集合」)を検索し、既存文献に同じものがないか確認すること。
  今回は2日分の記述("非自明サイクルを発見")が実際には2020年の論文の
  再発見だったと判明した。特に「一般化・拡張した設定で何かを見つけた」
  というときほど、その設定がどれだけ研究されているか(この場合は
  q=5の生成的コラッツ写像は2020年に専用の論文がある程度には研究されて
  いた)を過小評価しがちなので要注意。今後は「サイクルを発見した」ではなく
  必ず「サイクルを検出した(既知かどうかは要文献確認)」のように書き、
  文献確認が済むまでは新規性を主張しないこと。
